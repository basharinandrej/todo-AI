# Секретные ключи (чтобы ИИ случайно не украл или не слил ваши пароли)
.env
.env.*
config/secrets.json

# Огромные папки зависимостей (DeepSeek сломает зубы и потратит кучу токенов, если начнет их читать)
node_modules/
vendor/
.venv/

# Результаты сборки проекта и кэш
dist/
build/
.next/
.cache/

# Тяжелые медиафайлы и базы данных
*.mp4
*.png
*.jpg
*.sqlite
